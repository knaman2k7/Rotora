import db from "../database/db.ts";
import retrieveData from "./dataRetriever.ts";


export default function createRota(weekNo: number): Object{


    // retrieve and format the data
    var data = retrieveData(weekNo);

    // create the framework

    // run the algorithm

    // return the rota
    return {}


}